# Command Center

هذا الملف هو المدخل السريع لفهم المشروع.  
إذا بدأت جلسة جديدة هنا، اقرأ هذا الملف أولًا.

## ما هو المشروع

`Command Center` لوحة تحكم شخصية ثابتة منشورة على GitHub Pages.

الهدف منها:
- عرض المشاريع والخدمات والبوتات والأدوات والسحابة من مكان واحد
- التمييز بين الجرد الثابت في `data.js` والتحقق الدوري في `data.runtime.json`
- إعطاء صورة تشغيلية واضحة بدون ادعاء أن كل شيء live بنفس العمق

## الملفات الأساسية

- `index.html`
  - الغلاف الوحيد للواجهة
- `app.js`
  - كل منطق العرض، التنقل، التفاصيل، البحث، وربط runtime state
- `data.js`
  - مصدر الحقيقة اليدوي لكل الكيانات
- `style.css`
  - كل التصميم والـ layout والـ detail treatments
- `data.runtime.json`
  - آخر نتيجة تحقق محفوظة
- `runtime-sync.js`
  - checker الدوري الذي يبني `data.runtime.json`
- `runtime-sync-publish.sh`
  - يشغّل checker ثم يعمل `commit/push` فقط إذا تغير `data.runtime.json`
- `RUNTIME_SYNC.md`
  - توثيق تشغيل نظام التحديث الدوري

## كيف يُفهم النظام

هناك طبقتان فقط:

1. `data.js`
- التعريف اليدوي
- الأسماء
- الأنواع
- العلاقات
- المسارات
- الملخصات

2. `data.runtime.json`
- آخر تحقق محفوظ
- `ok / warn / fail / manual`
- وقت آخر تحقق
- مصدر التحقق

الواجهة تقرأ الاثنين معًا.

## الكيانات الرئيسية

- `PRJ`
  - المشاريع والأنظمة
- `SVC`
  - الخدمات والتشغيل على السيرفر
- `AUTO`
  - الأتمتة والمهام المجدولة
- `BOT`
  - البوتات والـ runtimes
- `TL`
  - الأدوات وبيئات AI CLI وMCP
- `CLD`
  - المنصات والخدمات السحابية والخارجية
- `ARC`
  - الأرشيف والمراجع النشطة
- `IDEAS`
  - الأفكار وخارطة الطريق

## قواعد العمل داخل هذا المشروع

- لا تضف ملفات مؤقتة أو TODOs جديدة إلا إذا كانت مطلوبة فعلًا أثناء التنفيذ ثم تُزال عند النهاية
- لا تكرر التوثيق في أكثر من ملف
- أي claim في الواجهة يجب أن يطابق:
  - `data.js`
  - أو `data.runtime.json`
- لا تغيّر مسارات التشغيل أو أسماء الملفات بدون سبب واضح
- إذا حسّنت الواجهة، افحص:
  - syntax
  - deep links
  - console errors
  - desktop/mobile

## التحديث الدوري

التحديث الدوري لا يتم من جلسة AI.

المشغل الحقيقي:
- `launchd` على macOS

المنطق:
- `runtime-sync.js`
- ثم `runtime-sync-publish.sh`

التفاصيل الكاملة:
- راجع `RUNTIME_SYNC.md`

## نقاط حساسة

- `data.runtime.json` ملف مولّد، لا يُحرر يدويًا إلا للتشخيص
- parsing الـ hash يعتمد الآن على `_hashParts()` في `app.js`
- deep links يجب أن تعمل من URL المباشر مثل:
  - `#/projects/Command Center`
  - `#/tools/Runtime Sync System`
  - `#/cloud/GitHub`

## عند بدء أي جولة عمل جديدة

اقرأ بهذا الترتيب:

1. `CLAUDE.md`
2. `RUNTIME_SYNC.md`
3. `data.js`
4. `app.js`
5. `style.css`

هذا يكفي لفهم المشروع قبل أي تعديل.
