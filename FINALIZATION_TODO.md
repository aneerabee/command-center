# Command Center Finalization TODO

تاريخ البدء: 2026-04-05
الحالة: قيد التنفيذ

هذا الملف ليس توثيقًا نظريًا. هو قائمة تنفيذ فعلية حتى إنهاء Command Center دون ترك أجزاء نصف مكتملة.

## 1) تدقيق الحقيقة في البيانات

- [x] مراجعة `PRJ` عنصرًا عنصرًا مقابل المصدر المحلي والسيرفر
- [x] مراجعة `SVC` مقابل Docker / systemd / cron / المسارات الفعلية
- [x] مراجعة `BOT` مقابل الواقع التشغيلي الحالي
- [x] مراجعة `TL` مقابل ملفات الإعداد الفعلية
- [x] مراجعة `CLD` مقابل الاستخدام الحقيقي الحالي
- [x] مراجعة `ARC` وإخراج أي عنصر لا ينتمي للأرشيف
- [x] مراجعة `IDEAS` والتأكد أن كل فكرة مرتبطة بنطاق وخطوة تالية واضحة
- [x] إزالة أي claim غير مدعوم أو عنصر مكرر أو ملتبس

## 2) تدقيق نظام التحديث الحي

- [x] مراجعة `runtime-sync.js` بالكامل
- [x] مراجعة معنى `ok / warn / manual` عبر كل الأنواع
- [x] مراجعة `data.runtime.json` والتأكد أن coverage مطابقة لما يكتبه checker
- [x] مراجعة `runtime-sync-publish.sh` ومنع أي سلوك قد يسبب نشرًا خاطئًا
- [x] مراجعة `launchd` والتوثيق المرتبط به
- [x] توحيد لغة الواجهة حتى لا تدّعي التغطية الكاملة بينما التحقق جزئي

## 3) صقل الصفحة الرئيسية

- [x] تحسين hierarchy في `home`
- [x] تقوية وضوح `trust` و`runtime coverage`
- [x] تقليل النصوص المضغوطة وتحويلها إلى blocks قابلة للمسح السريع
- [x] التأكد أن `home` تجيب سريعًا على: ماذا أملك / ما الذي يعمل / ما الذي ما زال يدويًا

## 4) توحيد كل detail views

- [x] مراجعة `project detail`
- [x] مراجعة `service detail`
- [x] مراجعة `bot detail`
- [x] مراجعة `tool detail`
- [x] مراجعة `cloud detail`
- [x] مراجعة `idea detail`
- [x] مراجعة `archive detail`
- [x] توحيد الترتيب: تعريف سريع → علاقات → ثقة/تحقق → تشغيل/وصول → مرجع

## 5) تحسين الخريطة والتنقل

- [x] مراجعة `map` لتكون خريطة تشغيل لا listing فقط
- [x] مراجعة deep links
- [x] مراجعة البحث والتنقل بين الأقسام
- [x] مراجعة الوضوح بين المحلي / السيرفر / السحابي / الحساس

## 6) التوثيق الداخلي داخل اللوحة

- [x] التأكد أن `Runtime Sync System` موثق داخل `tools` بشكل كافٍ
- [x] ربط التوثيق مع `home` و`automation` و`map`
- [x] التأكد أن أي مطور لاحق سيفهم: ما هذا / كيف يعمل / أين ملفاته / ما الذي يحدثه

## 7) تحقق نهائي

- [x] فحص syntax
- [x] فحص runtime checker
- [x] فحص Playwright desktop
- [x] فحص Playwright mobile
- [x] فحص console errors
- [x] مراجعة نهائية متعددة الزوايا
- [x] إصلاح آخر findings

## 8) الإغلاق

- [x] تحديث التوثيق النهائي داخل المشروع
- [x] التأكد أن الشجرة نظيفة بلا ملفات زائدة
- [ ] commit + push نهائي
