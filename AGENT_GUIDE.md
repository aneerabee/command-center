# دليل الوكلاء — إضافة بطاقات المتابعة الذكية

> هذا الملف مرجع دائم لأي وكيل Claude يعمل على مشروع موجود في `command-center`.
> الهدف: إضافة بطاقات "أين وصلنا" + "استئناف محادثة Claude" + "روابط سريعة بشرح" لمشروعك.

---

## كيف يعمل النظام

البنية التحتية (الدوال + CSS) **منشورة مسبقاً في `app.js` و `style.css`**.
الوكيل يضيف بيانات فقط في `data.js` — والواجهة ترسم البطاقات تلقائياً.

ثلاثة حقول اختيارية يقرأها الكود:

| الحقل | الغرض | يظهر كـ |
|------|------|---------|
| `current_status` | الموقف الحالي + الخطوة التالية + العوائق | بطاقة بنفسجية "📍 أين وصلنا" |
| `claude_session` | معلومات استئناف محادثة Claude Code | بطاقة خضراء "💬 استئناف محادثة" |
| `links_desc` | شرح صغير لكل رابط في `links` | يُحقن في "🔗 روابط سريعة" |

**القاعدة الذهبية:** أي حقل غير معروف → احذفه. الكود يتعامل مع غيابه بأمان.

---

## ❌ ممنوعات صارمة

- لا تعدّل `app.js` نهائياً
- لا تعدّل `style.css` نهائياً
- لا تدفع لـ GitHub بدون إذن صريح من المستخدم
- لا تخمّن قيماً غير مؤكدة — اسأل المستخدم

---

## الخطوات بالترتيب

### 1) استنساخ المستودع

```bash
cd /tmp && rm -rf command-center && gh repo clone aneerabee/command-center
```

### 2) إيجاد المشروع في `data.js`

ابحث عن مشروعك بالاسم. لو غير موجود، اسأل المستخدم: "هل أضيفه كمشروع جديد أم تستخدم اسماً آخر؟"

### 3) جمع المعلومات من المستخدم

اسأل أسئلة مركّزة بالترتيب — لا تخمّن:

**(أ) لـ `claude_session`:**
- "ما اسم جلسة Claude لهذا المشروع؟" (يُستعمل مع `/resume`). لو لا يوجد، اقترح `/rename` أولاً.
- "أي تيرمينال تستعمله؟" (Warp / iTerm / Terminal.app / Core Shell / Cursor / Ghostty …)
- "ما المسار الكامل لمجلد المشروع؟"
- "هل يوجد ملف ذاكرة دائم في `~/.claude/projects/.../memory/`؟" — لو لا، احذف الحقل.
- "ما آخر نقطة وصلتم إليها؟" (جملة قصيرة لـ `note`)

**(ب) لـ `current_status`:**
- "أين وصلت في المشروع حالياً؟" (فقرة قصيرة)
- "ما الخطوة التالية بالضبط؟"
- "ما العوائق الحالية؟ ولكل عائق، ما درجة أهميته؟" (`high` / `med` / `low`)
- "كيف يستخدم الناس هذا المشروع يومياً؟"

**(ج) لـ `links` و `links_desc`:**
- راجع الروابط الموجودة في `links` — هل تنقص روابط مهمة؟
- أمثلة لما قد ينقص: لوحة النشر، Console الخدمة الخارجية، Dashboard المراقبة، صفحة الإعدادات.
- لكل رابط، اسأل: "ما الغرض في جملة قصيرة؟" — للحقل `links_desc`.

### 4) إضافة الحقول الثلاثة

داخل كائن المشروع في `data.js`، **بعد حقل `links` الموجود**:

```javascript
// وسّع/راجع links الموجود ثم أضف:
links_desc: {
  "اسم-الرابط-1": "شرح قصير ماذا يفعل ومتى يُستخدم",
  "اسم-الرابط-2": "...",
  // الأسماء يجب أن تطابق مفاتيح links بالضبط
},

current_status: {
  updated: "2026-MM-DD",
  where: "الموقف الحالي بفقرة عربية واضحة (3-5 أسطر).",
  next_step: "الخطوة التالية بالضبط.",
  blockers: [
    { text: "وصف العائق", priority: "high" },  // high | med | low
    { text: "...", priority: "med" },
  ],
  use_guide: "دليل قصير: للاستخدام اليومي ... للتطوير ... للنشر ...",
},

claude_session: {
  session_name: "[اسم-الجلسة]",
  terminal: "اسم التيرمينال (Warp / iTerm2 / Core Shell / ...)",
  cwd: "/المسار/الكامل/للمشروع",
  command: "cd <المسار> && claude",       // لو فيه مسافات/إيموجي استعمل \\
  memory_file: "~/.claude/projects/.../memory/<file>.md",  // اختياري — احذفه إن لم يوجد
  note: "تذكير قصير عن آخر نقطة وصلتم لها",
},
```

### 5) التحقق من سلامة JS

```bash
cd /tmp/command-center && node -c data.js && echo "✓ سلامة"
```

لو فيه خطأ syntax، اقرأ السطر بالضبط وصلّحه. **لا تنتقل للخطوة التالية إلا بعد ✓.**

### 6) معاينة محلية

```bash
cd /tmp/command-center && python3 -m http.server 8765 --bind 127.0.0.1 &
```

أعطِ المستخدم الرابط — استبدل المسافات في اسم المشروع بـ `%20`:

```
http://127.0.0.1:8765/#/projects/<اسم-المشروع-بالضبط>
```

### 7) تأكيد بصري

اطلب من المستخدم يفتح الرابط ويتحقق:
- هل البطاقات الثلاث ظاهرة؟
- هل المعلومات صحيحة؟
- هل يحتاج تعديل قبل الدفع؟

### 8) دفع لـ GitHub (فقط بعد إذن صريح بكلمة "ادفع")

```bash
cd /tmp/command-center && \
  git add data.js && \
  git commit -m "feat(data): add status + resume + links_desc for <PROJECT_NAME>" && \
  git push origin main
```

---

## نقاط احتكاك شائعة

| الموقف | الحل |
|--------|------|
| المسار فيه مسافات/إيموجي | في `command` استعمل `\\`: `cd ~/Desktop/My\\ Project && claude` |
| لا يوجد ملف ذاكرة | احذف حقل `memory_file` بالكامل |
| المستخدم يستعمل تيرمينالين | اكتبهما معاً: `"Warp / Cursor"` |
| المشروع غير موجود في `data.js` | راجع مشروع موجود كقالب |
| لا توجد عوائق حالياً | `blockers: []` — القسم يُخفى تلقائياً |
| `priority` غير معروف | استعمل `"med"` كاحتياط آمن |
| `updated` غير معروف | استعمل تاريخ اليوم بصيغة YYYY-MM-DD |

---

## القالب المعتمد

افتح `data.js` وابحث عن `id: "whatsapp-crm"` — هذا هو **القالب المرجعي الكامل**.
انسخ بنيته بالضبط واستبدل القيم بقيم مشروعك.

---

## أيقونات الروابط (تلقائية حسب الاسم)

الكود يربط أيقونات تلقائياً لأسماء روابط شائعة. لو اسمك من هذه القائمة ستظهر أيقونته:

| الاسم | الأيقونة |
|-------|---------|
| GitHub | 💻 |
| Railway | 🚂 |
| Supabase | 🗄️ |
| Vercel | ▲ |
| Hostinger | 🌐 |
| Telegram | ✈️ |
| Notion | 📝 |
| Meta Console | 🛠️ |
| WhatsApp Manager | 💬 |
| Events Manager | 📊 |
| App Review | ✅ |
| API Setup | 📡 |
| التطبيق | 🚀 |
| لوحة التحكم | 📊 |
| صفحة الربط | 🔗 |

أي اسم آخر → أيقونة افتراضية 🔗.

---

## لمن يطوّر النظام نفسه (لا للوكلاء العاديين)

إذا أردت توسيع البطاقات أو إضافة بطاقة جديدة:

- **`_currentStatusCard(item)`** في `app.js` — رسم بطاقة الحالة
- **`_claudeSessionCard(item)`** في `app.js` — رسم بطاقة استئناف Claude
- **`_quickLinksCard(item, cl)`** في `app.js` — رسم الروابط السريعة
- جميع الـ CSS في نهاية `style.css` تحت تعليقات `/* ══════ ... ══════ */`

استدعاؤها من `openProjectDetail()` بهذا الترتيب: status → claude → grid → quick-links.
